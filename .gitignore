# ============================================================
#  NEVER upload secrets. These stay on your machine / server only.
# ============================================================
*.key
kalshi-demo.key
kalshi-live.key
config.yaml
config_active.yaml
config_balanced.yaml
config_live.yaml

# local runtime + build artifacts (no need to sync)
logs/
__pycache__/
*.pyc
kalshibot_cloud.tar.gz
KalshiPaperBot_share.zip
cloud_oneshot.sh
autopush_hidden.flag
github_token.txt
